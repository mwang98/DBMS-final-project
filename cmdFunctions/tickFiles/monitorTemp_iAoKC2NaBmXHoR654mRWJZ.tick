dbrp "monitorTemp_iAoKC2NaBmXHoR654mRWJZ"."autogen"
var data = stream
    |from()
        .measurement('temperature')
    |window()
        .period(10s)
        .every(10s)

data
    @tTest()
        // specify the hotend field
        .field('hotend')
        // Keep a 1h rolling window
        .size(3600)
    |alert()
        .id('hotend')
        .crit(lambda: 0 < 100)
        .log('/tmp/kapacitor_udf/monitorTemp_iAoKC2NaBmXHoR654mRWJZ_failure.log')
        |influxDBOut()
            .database('Mike')
            .retentionPolicy('autogen')
            .measurement('monitorTemp_iAoKC2NaBmXHoR654mRWJZ_alert')
    
data
    |influxDBOut()
        .create()
        .database('Mike')
        .retentionPolicy('autogen')
        .measurement('monitorTemp_iAoKC2NaBmXHoR654mRWJZ')
        .tag('hotend', 'a')
        .tag('bed', 'b')
        .tag('air', 'c')

